let initialState = {
    data: {
        id: 6722,
        login: "WhiteHoney",
        email: "vanove2013@yandex.ru"
    }
};

const authReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default authReducer;